import React from 'react';
import ReactDOM from 'react-dom';
//import {Route} from 'react-router';
//import {createHistory} from 'history';

import App from './app/MdoxApp';
// <App history={history} />
//const history = createHistory();

ReactDOM.render(
	<App />,
	document.getElementById('root')
);
